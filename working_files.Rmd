---
title: "Working with files"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{bash}
cd ~/data-shell
mkdir -p thisis
```

```{bash}
ls -F ~/data-shell
```
```{bash}
cd ~/data-shell
mkdir -p ~/temp/one/two/three
ls temp
```

# Stopped before "Moving files and directories" on 9/11/2020






